const createUser = async (user) => {
  return {};
};

const getUserById = async (id) => {
  return {};
};

const getUsers = async () => {
  return [];
};

export default {
  createUser,
  getUserById,
  getUsers,
};
